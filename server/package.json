{
  "name": "zoomapi-server",
  "version": "1.0.0",
  "private": true,
  "description": "Zoom API Backend",
  "main": "server.js",
  "scripts": {
    "build": "cd client && npm run build",
    "start-server": "cd server && node start.js",
    "start": "npm run build && npm run start-server"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
